# Tu ritmo

La última vez que hiciste cada cosa. Lo escribe tu asistente solo, al terminar
cada comando. No hace falta que lo edites.

Lo único que puedes escribir tú: la palabra `pausado` al final de una línea, si no
quieres que te la vuelva a mencionar. Por ejemplo: `- arranca: 2026-09-08 pausado`.

- arranca: nunca
- siguiente-nivel: nunca
- auditoria: nunca
- conexion probada: nunca
- respaldo: nunca
- plan pendiente: ninguno
